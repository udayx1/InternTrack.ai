# Base URL for the Stage 2 backend API. All services read from this — never hard-code URLs.
VITE_API_BASE_URL=http://localhost:5000/api

# When true, services use in-memory mock data instead of calling the API.
# Flip to false once the Stage 2 backend is live.
VITE_USE_MOCKS=true
